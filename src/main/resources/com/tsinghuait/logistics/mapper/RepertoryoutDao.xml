<?xml version="1.0" encoding="UTF-8" ?>
  <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tsinghuait.logistics.mapper.RepertoryoutDao">
	<insert id="insertRepertoryout" parameterType="repertoryout">
		insert into
		repertoryout(roId,orderNumber,roTime,destination,transportation,checkstate)
		values(#{roId},#{orderNumber},#{roTime},#{destination
		},#{transportation},#{checkstate})
	</insert>
	<select id="selectAllRepertoryout" resultType="repertoryout">
		SELECT * FROM
		REPERTORYOUT;
	</select>
</mapper>